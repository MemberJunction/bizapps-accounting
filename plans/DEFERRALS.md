# DEFERRALS — bizapps-accounting

Time/dependency deferrals of master-plan scope. Per the planning system (§5.2, 2026-07-14): a deferral is
a plan-time sequencing decision, NOT a master-plan contradiction. Entry: item · source · rationale ·
revisit trigger.

| Deferred item | Source | Rationale | Revisit trigger |
|---|---|---|---|
| **Timing/period restriction system** (close guard or equivalent — the successor to the removed AccountingPeriod / withdrawn MOD-13) | MOD-1 (final) + MOD-13 (withdrawn) + Marcelo 2026-07-14 | a real gap, deferred: restrictions aren't needed for baseline testing (first test sets run unrestricted); batch summaries lose date info; accountants batch entries into the right periods manually for now | restriction requirements emerge — Robert's periods research, Jeremy's Q19f answer, or cutover operations. Design constraint locked: detect by DATE, never a period FK |
| `AccountBalance` / `AccountBalanceByDimension` materialization (§4.10, BA-D22) | MOD-2 (Amith: "might kill for v1") | §10 views compute balances on demand | read-model performance demands it |
| Tax CALCULATION provider + adapters — Avalara/TaxJar/Local (§9) | master Block-7 sequencing + open Robert tax-shape decision | tax DATA tables are built; the quick path (tax-as-order-line) may serve v0 | Robert's tax decision + a calculating consumer |
| Unrealized-FX revaluation + reporting-currency translation (§6.4/§6.5) | MOD-6 (all FX upstream) | FX itself is deferred (orders MOD-4); accounting keeps only refs + vw_FxExposure | multi-currency activates |
| Report Gallery app (§10.3) | master's own out-of-scope | referenced for completeness only | ecosystem-level decision |
| Intercompany receiving-contract tests (feature B4) | accounting MOD-5 (Payments owns legs + wiring) | nothing to receive until Payments/O2 exists | orders O2 lands |
| **Reports category — the 7 report pages** (AR Aging · DefRev Rollforward · Trial balance (AR) · AR↔GL recon · GL detail · Dimension P&L · Sales tax liability) + the shared report-page scaffold (UI plan §8.5) | Marcelo, 2026-07-16 — *"defer the reports pages for my go later to prioritize the impact pages"* | Planned and MOCKED (all 7 approved in the mockup set), just sequenced behind the impact screens. NOT dropped. The Reports CATEGORY + rail ship now, with Trial balance (AR) + Sales tax liability wired to the existing read-model dashboards that already answer them; the other five rail items show the honest not-built state | **Marcelo's explicit go.** He said he wants to slice the review feature-by-feature first |
| **Users & roles + Approvals settings** (UI plan §8.4) | §8.6 build order + the §8 dispatch ruling ("build LAST … stubs acceptable") | Gated on external decisions, not on effort: **A2** roles/RLS is Marcelo co-design (see [Q22](QUESTIONS.md#q22)/[Q24](QUESTIONS.md#q24)) and the **C.8** approval-policy shape is unsettled ([Q6](QUESTIONS.md#q6)). Building to a guess would bake in the wrong model. The Configuration category + rail ship now; both pages show the not-built state | A2 lands (Robert answers Q22/Q24 → the company-access mechanism) AND the C.8 policy shape is ruled |
| **`mj-left-nav` desktop icons-only collapse** (the approved mockup's hamburger rail) | [Q27](QUESTIONS.md#q27) — MJ-wins adoption of `<mj-left-nav>` at the §8.0 shell build | MJ's canonical rail has no desktop collapse (its narrow-viewport answer is an off-canvas drawer). We adopted MJ's component rather than fork it, so the mockup's hamburger does not ship. Not a blocker — the rail is 240px and laptop-width tolerance holds | Matt rules on Q27. If MJ accepts a `[Collapsible]` input we contribute it upstream; if not, the always-expanded rail is permanent and the hamburger is struck from the design record |
| **Server-side (keyset/infinite) paging on the JE list** | MJ-UPSTREAM: `mj-entity-data-grid` renders a permanent "No data to display" overlay in `PaginationMode="infinite"` | The UI plan wants keyset "Load more" for the thousands-of-JEs case. MJ's infinite mode is the idiom for it, but its empty-state is gated on `rowData.length`, which infinite mode never populates. We ship `PaginationMode="client"` meanwhile — honest and correct at our current volume | MJ fixes the infinite-mode empty state (logged in `~/MJDev/MJ-UPSTREAM.md` with the one-line fix), OR our JE volume grows enough that client-side paging hurts |
| **Tier-4 in-process real-DB binding** (`setupSQLServerClient`, the tier-2 path) | TEST-ARCHITECTURE tier 4 + the 2026-07-16 tier-4 stand-up | The tier-4 harness is live and its keystone is proven, but component `RunView`s are not yet bound to the real in-process DB — the first spec's data came from an injected service. Until wired, tier-4 green says nothing about the data path (tier 2/3 own that) | The tier-4/5 wrap-up, and/or Marcelo's ruling on whether mjdev/MJ owns the harness (`~/MJDev/MJDEV-REQUESTS.md`) |
