---
"@mj-biz-apps/accounting-ng": patch
---

Batch dispatch dashboard: a first dispatch that the ERP rejects now shows as an error pointing at Dispatch status, instead of a success message reading "→ Failed". Only a `Posted` outcome is reported as success (#192).
