{
  // Re-includes the specs the build tsconfig excludes, for `test:types` — vitest itself only
  // transpiles, so without this a spec's type errors are never reported.
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "types": ["node"]
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules"]
}
