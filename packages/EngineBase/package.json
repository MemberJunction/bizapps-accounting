{
  "name": "@mj-biz-apps/accounting-engine-base",
  "type": "module",
  "version": "0.1.0",
  "description": "Browser-safe accounting engine base for BizApps Accounting — metadata caches (GL accounts, roles, links, dimensions, company profiles), the ResolveLinkedAccount primitive, and the pure CreateJournalEntry draft-validation pipeline + typed contract shared with the server engine and all callers (Orders, browsers, scripts).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "/dist"
  ],
  "scripts": {
    "build": "tsc && tsc-alias -f",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "author": "MemberJunction.com",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.0.18",
    "vite": "^7.1.5"
  },
  "dependencies": {
    "@mj-biz-apps/accounting-entities": "0.1.0"
  },
  "peerDependencies": {
    "@memberjunction/core": "^5.50.0",
    "@memberjunction/global": "^5.50.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/MemberJunction/bizapps-accounting"
  }
}
