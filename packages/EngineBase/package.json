{
  "name": "@mj-biz-apps/accounting-engine-base",
  "type": "module",
  "version": "0.4.0",
  "description": "Browser-safe accounting engine base for BizApps Accounting — metadata caches (GL accounts, roles, links, dimensions, company profiles), ResolveLinkedAccount / ResolveLinkedAccounts (BA-D34 cardinality), the pure CreateJournalEntry draft-validation pipeline + typed contract, and NetLines (pure JE rollup) shared with the server engine and all callers (Orders, browsers, scripts).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "/dist"
  ],
  "scripts": {
    "build": "tsc && tsc-alias -f",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "author": "MemberJunction.com",
  "license": "BUSL-1.1",
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.0.18",
    "vite": "^7.1.5"
  },
  "dependencies": {
    "@mj-biz-apps/accounting-entities": "0.4.0"
  },
  "peerDependencies": {
    "@memberjunction/core": "^6.1.0-edge.4",
    "@memberjunction/global": "^6.1.0-edge.4"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/MemberJunction/bizapps-accounting"
  }
}
